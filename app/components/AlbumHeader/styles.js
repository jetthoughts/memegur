import { StyleSheet } from 'react-native';
import colors from '../../constants/Colors';

export default StyleSheet.create({
  iconStyle: {
    color: 'white',
    padding: 5,
  },
  topNavBar: {
    backgroundColor: colors.postBackground,
    borderBottomColor: colors.postBackground,
  },
});
